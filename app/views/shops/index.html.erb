<div class="container">
  <div class="title">
    <h1>Shop</h1>
  </div>
  <div >
    <ul class="shops">
    <% @shops.each do |shop| %>
      <li>
        <%= link_to shop.name, shop_path(shop) %>
      </li>
    <% end %>
  </ul>
  </div>
</div>
